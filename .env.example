PORT=3050
MONGO_URI=mongodb+srv://fincoord-admin:H$tg5652@fincoord.eprxpqd.mongodb.net/?appName=fincoord
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=7d

# ── Optional: SMTP for sending password-reset emails ────────────────────────
# Uncomment and fill these in production to enable email delivery instead
# of returning the reset token in the API response.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@fincoord.app
